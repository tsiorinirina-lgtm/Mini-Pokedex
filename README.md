# Mini-Pokedex

This is a React Practice project.  
Only the fetch JavaScript API was used in this project.  
If you want to test it, open a terminal in the project directory and run:

> `npm install`  
> `npm run dev`
